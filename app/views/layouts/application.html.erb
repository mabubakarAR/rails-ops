<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RailsOps Job Portal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <% unless controller_name == 'sessions' || controller_name == 'registrations' %>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
        <div class="container">
          <%= link_to "RailsOps", root_path, class: "navbar-brand fw-bold fs-4" %>
          
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
              <% if user_signed_in? %>
                <!-- User Info -->
                <li class="nav-item me-3">
                  <div class="d-flex align-items-center">
                    <div class="bg-white bg-opacity-20 rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 35px; height: 35px;">
                      <i class="fas fa-user text-white"></i>
                    </div>
                    <span class="navbar-text text-white fw-medium">
                      Welcome, <%= current_user.full_name %>
                    </span>
                  </div>
                </li>
                
                <!-- Navigation Links -->
                <li class="nav-item">
                  <%= link_to "Jobs", jobs_path, class: "nav-link fw-medium" %>
                </li>
                
                <% if current_user.company? %>
                  <% if current_user.company.present? %>
                    <li class="nav-item">
                      <%= link_to "My Jobs", jobs_path, class: "nav-link fw-medium" %>
                    </li>
                    <li class="nav-item">
                      <%= link_to "My Applications", job_applications_path, class: "nav-link fw-medium" %>
                    </li>
                  <% else %>
                    <li class="nav-item">
                      <%= link_to "Create Company Profile", new_company_path, class: "nav-link fw-medium" %>
                    </li>
                  <% end %>
                <% elsif current_user.job_seeker? %>
                  <% if current_user.job_seeker.present? %>
                    <li class="nav-item">
                      <%= link_to "My Applications", job_applications_path, class: "nav-link fw-medium" %>
                    </li>
                  <% else %>
                    <li class="nav-item">
                      <%= link_to "Create Profile", new_job_seeker_path, class: "nav-link fw-medium" %>
                    </li>
                  <% end %>
                <% end %>
                
                <!-- Logout Button -->
                <li class="nav-item">
                  <form action="<%= destroy_user_session_path %>" method="post" style="display: inline;">
                    <input type="hidden" name="_method" value="delete">
                    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                    <button type="submit" class="nav-link btn btn-link text-decoration-none border-0 bg-transparent fw-medium" style="color: rgba(255,255,255,.8);">
                      <i class="fas fa-sign-out-alt me-1"></i>Logout
                    </button>
                  </form>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "Login", new_user_session_path, class: "nav-link fw-medium" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Sign Up", new_user_registration_path, class: "nav-link fw-medium" %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="<%= controller_name == 'sessions' || controller_name == 'registrations' ? '' : 'container mt-4' %>">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
